{
  "name": "ai-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first context portability across AI providers.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check": "node scripts/check-repository.mjs",
    "test": "node --test",
    "build:browser": "node scripts/build-browser.mjs"
  }
}
